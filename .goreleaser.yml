# This is an example goreleaser.yaml file with some sane defaults.
# Make sure to check the documentation at http://goreleaser.com
project_name: warp

builds:
  -
    goos:
      - freebsd
      - windows
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
      - --tags=kqueue
    ldflags:
      - -s -w -X github.com/minio/warp/pkg.ReleaseTag={{.Tag}} -X github.com/minio/warp/pkg.CommitID={{.FullCommit}} -X github.com/minio/warp/pkg.Version={{.Version}} -X github.com/minio/warp/pkg.ShortCommitID={{.ShortCommit}} -X github.com/minio/warp/pkg.ReleaseTime={{.Date}}
archives:
  -
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE
      - warp_logo.png
checksum:
  name_template: 'checksums.txt'
snapshot:
  name_template: 'snapshot-{{ time "2006-01-02" }}'
changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'
nfpms:
  -
    vendor: Hanzo AI, Inc.
    homepage: https://github.com/hanzos3/warp
    maintainer: Hanzo AI <dev@hanzo.ai>
    description: Hanzo S3 Warp - S3 API Benchmark Tool
    license: GNU Affero General Public License v3.0
    formats:
      - deb
      - rpm
    contents:
      # Basic file that applies to all packagers
      - src: systemd/warp.service
        dst: /etc/systemd/system/warp.service
    file_name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
dockers:
  -
    # GOOS of the built binary that should be used.
    goos: linux
    # GOARCH of the built binary that should be used.
    goarch: amd64
    dockerfile: Dockerfile.release
  - image_templates:
      - "ghcr.io/hanzos3/warp:{{ .Tag }}-amd64"
    use: buildx
    goarch: amd64
    ids:
      - warp
    dockerfile: Dockerfile.release
    build_flag_templates:
      - "--platform=linux/amd64"
  - image_templates:
      - "ghcr.io/hanzos3/warp:{{ .Tag }}-arm64"
    use: buildx
    goarch: arm64
    ids:
      - warp
    dockerfile: Dockerfile.release
    build_flag_templates:
      - "--platform=linux/arm64"
  - image_templates:
      - "hanzos3/warp:{{ .Tag }}-amd64"
    use: buildx
    goarch: amd64
    ids:
      - warp
    dockerfile: Dockerfile.release
    build_flag_templates:
      - "--platform=linux/amd64"
  - image_templates:
      - "hanzos3/warp:{{ .Tag }}-arm64"
    use: buildx
    goarch: arm64
    ids:
      - warp
    dockerfile: Dockerfile.release
    build_flag_templates:
      - "--platform=linux/arm64"
docker_manifests:
  - name_template: hanzos3/warp:{{ .Tag }}
    image_templates:
      - hanzos3/warp:{{ .Tag }}-amd64
      - hanzos3/warp:{{ .Tag }}-arm64
  - name_template: hanzos3/warp:latest
    image_templates:
      - hanzos3/warp:{{ .Tag }}-amd64
      - hanzos3/warp:{{ .Tag }}-arm64
  - name_template: ghcr.io/hanzos3/warp:{{ .Tag }}
    image_templates:
      - ghcr.io/hanzos3/warp:{{ .Tag }}-amd64
      - ghcr.io/hanzos3/warp:{{ .Tag }}-arm64
  - name_template: ghcr.io/hanzos3/warp:latest
    image_templates:
      - ghcr.io/hanzos3/warp:{{ .Tag }}-amd64
      - ghcr.io/hanzos3/warp:{{ .Tag }}-arm64
